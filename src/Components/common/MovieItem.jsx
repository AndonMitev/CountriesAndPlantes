import React, { Fragment } from 'react';

const MovieItem = props => {
    const { movie } = props;
    console.log(movie);

    return (
        (
            <Fragment>

            </Fragment>
        )
    )
}

export default MovieItem;